<h1>メンバー追加</h1>
<%= form_with model: @member, local: true do |form| %>
  <div>
    <%= form.label :name, "name"%>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= from.submit "送信" %>
  </div>
<% end %>
