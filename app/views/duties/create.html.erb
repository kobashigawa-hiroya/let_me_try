<h1>メンバー追加</h1>
<%= form_with model: @duties, local:true do |form| %>
  <div>
    <%= form.label :title, "title"%>
    <%= form.text_field :title %>
  </div>
  <div>
    <%= from.submit "送信" %>
  </div>
<% end %>
